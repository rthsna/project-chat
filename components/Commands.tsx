import React from 'react'

const Commands = () => {
    const command = ""

    function CheckCommand(msg){
        switch(msg){
            case "/slot":
                console.log("command slot")
                break
        }
    }

  return (
    <div>
      
    </div>
  )
}

export default Commands
